import { StyleSheet } from 'react-native';

export const HIDDEN_STYLE = StyleSheet.create({ hidden: { height: 0 } }).hidden;

export const CARD_BASE_STYLE = StyleSheet.create({
  card: { borderRadius: 14, overflow: 'hidden' },
}).card;

export const CARD_IMAGE_STYLE = StyleSheet.create({
  image: { borderRadius: 14 },
}).image;
